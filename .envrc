# Inherit the global environment (~/.envrc) via direnv. This repo defines no
# secrets of its own; optional runtime knobs (ZOTERO_WRITE_API_TOKEN,
# ZOTERO_LOCAL_BASE_URL, ZOTERO_LIBRARY_ID, ZOTERO_PROFILE_DIR, EXPECTED_VERSION)
# live in ~/.envrc. Run `direnv allow` after cloning.
source_up
